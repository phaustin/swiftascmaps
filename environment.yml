name: notebook
channels:
  - eoas_ubc
  - conda-forge
dependencies:
  # NOTE: pip required for running tests in this repo
  - python=3.8
  - pip=20
  - numpy
  - scipy
  - matplotlib
  - jupytext
  - conda-lock
  - conda-build
  - anaconda-client
  - conda-verify
# conda-lock -f environment.yml -p linux-64
